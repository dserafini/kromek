# ifndef ENUMS_HH
# define ENUMS_HH 1

namespace Tuples {
  enum {
    kEvents,
    kDetector
  };
}

namespace TEvents {
  enum {
    kEvents,
  };
}

namespace TGeneration {
  enum {
    kEini,
    kVertexX,
    kVertexY,
    kVertexZ,
    kEventID
  };
}

namespace TDetector {
  enum {
    kEdep,
    kGammaX,
    kGammaY,
    kGammaZ
  };
}

# endif
